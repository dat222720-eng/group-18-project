- backend
- Khởi tạo server Express, cấu hình CORS, morgan, dotenv
- REST API /users với CRUD: GET/POST/PUT/DELETE
- Kết nối MongoDB qua Mongoose (model User { name, email })
- Viết hướng dẫn `.env` + cách chạy
cd backend
npm install
npm run dev
